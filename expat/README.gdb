Expat is not part of the GNU Debugger.  It is included as a convenience,
because GDB uses it.  Only minimal modifications have been made to this
copy (see ChangeLog.gdb); the changes are needed to build expat along with
GDB, but an unmodified expat can be used instead if it is built separately.

