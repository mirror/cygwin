# mep testcase for lbu $rn,$disp16($rm) -*- Asm -*-
# mach: all

	.include "testutils.inc"
	start
	.global lbu16
lbu16:
	# This instruction is exercised by the sb16 test case.
	mov $0, 0
	lbu $0,0($0)
	pass
