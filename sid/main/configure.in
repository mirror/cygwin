dnl Process this file with autoconf to produce a configure script.
AC_INIT(Makefile.in)
AC_CONFIG_AUX_DIR(../..)
AM_INIT_AUTOMAKE(sid,0.1)

dnl automake support
AM_MAINTAINER_MODE
AC_EXEEXT

AC_CONFIG_SUBDIRS(dynamic)

AC_OUTPUT(Makefile)
